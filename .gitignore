# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage/
htmlcov/

# Virtual environments
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
.env.local
.env.*.local

# Docker
docker/data/

# Generated backtest results + cached FX data (Fase 3/4)
results/
# data/ohlcv/  # qualification tests need these

# Experiment artifacts — search runs write their own SQLite store per round
# (experiments.db, r4_search.db, ...), all regenerable from the runners.
experiments/*/
experiments/*.db

# Checkpoints from GA runs
checkpoints/legacy/
checkpoints/pb_seed*/
checkpoints/*.json

# Agent / tooling local state
.omx/
.omc/
.omo/
.lean-ctx/
.superpowers/

# TypeScript incremental build cache
*.tsbuildinfo

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# ── GSD baseline (auto-generated) ──
.gsd
.gsd-worktrees/
.gsd-backups/
.gsd-id
.mcp.json
.bg-shell/
nul
nul.*
con
con.*
prn
prn.*
aux
aux.*
com[1-9]
com[1-9].*
lpt[1-9]
lpt[1-9].*
*~
*.code-workspace
.env.*
!.env.example
node_modules/
.next/
*.pyc
target/
vendor/
.cache/
tmp/
logs/
checkpoints/*.json
data/ohlcv/*.parquet
experiments/*.db
experiments/*.db.*-bak
experiments.db

# Data lake bars: 2.7 GB, rebuildable from the 1m parquet via
# scripts/resample_lake.py. The coverage/manifest JSON stays tracked so the
# lake's shape is reviewable without the payload.
data/lake/**/*.parquet

# Pinned datasets (track for reproducibility — see data/pinned/*provenance.json)
!data/pinned/
